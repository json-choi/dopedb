// The public origin this Worker is routed at. Crawler-facing metadata (robots
// host, Open Graph image base) must be absolute and identical across routes, so
// it is a checked-in constant rather than a request- or env-derived value.
export const workspaceSiteUrl = "https://app.dopedb.dev";
